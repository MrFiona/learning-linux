1、去除文件1.txt中的所有数字
cat 1.txt | sed 's/\([0-9]\)\([a-zA-Z]*\)/\2/g'
或者
cat 1.txt | sed 's/[0-9]//g'

2、去除文件中的所有字母、空格、空行以及！号
cat 1.txt | sed 's/[a-zA-Z!]//g' | sed 's/ //g' | sed '/^$/d'

注意：测试的时候发现，如果文本中有很多的空格，在不去掉空格的情况下用^$无法完成去掉空行的功能？？思考为什么？？